BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Major] ADD [coreSubjects] NVARCHAR(max),
[includedSpecializations] NVARCHAR(max);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
