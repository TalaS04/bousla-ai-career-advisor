BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[University] ADD [businessCode] NVARCHAR(1000),
[imageUrl] NVARCHAR(1000),
[logoUrl] NVARCHAR(1000),
[ministryId] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
